Using the attached screenshot as the exact style and content reference, extract and redraw the following elements from it, cleanly and at high resolution, in exactly the same style, same colours, same lighting and same angle as in the reference. Place them side by side in a single row on a pure flat MEDIUM GREY background (RGB 128,128,128), evenly spaced, with a LARGE clear gap between each element so that none of them touch or overlap. No ground, no drop shadows on the background, nothing else in the image. Elements, all EMPTY with NO text, NO numbers and NO digits inside them: (1) the white rounded capsule pill used for currencies in the top bar, blank, with a small round green plus button at its right end; (2) the yellow lightning-bolt energy icon on its own; (3) the red heart icon on its own; (4) the purple gem icon on its own; (5) the gold coin icon on its own; (6) the white rounded square button with a violet gear icon.
